export enum LexemeEntryTypeConst {
  Headword = 1,
  SubEntry = 2
}

const filesBaseUrl = 'https://stuartmcgill.github.io/cicipu-dict-files/'

export const AudioUrl = filesBaseUrl + 'audio_visual/'
export const ImagesUrl = filesBaseUrl + 'images/'
